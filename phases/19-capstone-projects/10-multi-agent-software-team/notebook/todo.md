# Capstone 10 — 多智能体软件工程团队 · 开发清单

> 对齐 `docs/zh.md` 构建步骤 + 练习 + 评分标准

---

## 构建步骤（按工程顺序）

### ✅ 步骤 1：任务板
- [x] `board/message.py` — MsgKind 枚举 + Message 数据类 + Subtask
- [x] `board/board.py` — Board 类（post/inbox/total_tokens）
- [x] `board/__init__.py` — 包导出

### ✅ 步骤 2：架构师
- [x] `roles/architect.py` — Architect 类，读 issue → 输出子任务列表
- [x] `config.py` — LLM 客户端 + 环境变量配置

### ⚠️ 步骤 3：编码者（部分完成）
- [x] `roles/coder.py` — Coder 类，领子任务 → 生成代码 diff
- [ ] 真实沙箱隔离（当前无 git worktree / Daytona）
- [ ] 并行执行（当前串行循环）

### ⚠️ 步骤 4：合并协调者（简化版）
- [x] 编排器直接拼接 diff（已实现）
- [ ] 独立合并协调者角色（课程要求）
- [ ] 三方合并 + LLM 介导冲突解决

### ✅ 步骤 5：评审者
- [x] `roles/reviewer.py` — Reviewer 类，读 diff → 批准/驳回
- [ ] 驳回后重试逻辑（当前驳回直接跳过）

### ⚠️ 步骤 6：测试者（部分完成）
- [x] `roles/tester.py` — Tester 类，LLM 分析代码
- [ ] 真实测试套件执行（当前只用 LLM 判断）

### ❌ 步骤 7：交接记账
- [ ] 每条跨角色消息记录 span（payload 大小、模型、Token）
- [ ] 计算 Token 放大 = (编码者_tokens + 评审者_tokens + 测试者_tokens + 架构师_share) / 编码者_tokens
- [ ] 输出交接日志（JSON 或 Langfuse span）

### ❌ 步骤 8：评测循环
- [ ] 定义 10 个测试任务（从简单到复杂）
- [ ] 每个任务运行 `run_team()` 多 Agent
- [ ] 每个任务运行 `single_agent_baseline()` 单 Agent
- [ ] 记录 pass@1、Token 消耗、挂钟时间、交接次数
- [ ] 输出 `eval_results.json`

### ❌ 步骤 9：事后分析
- [ ] 识别断裂的交接（计划太模糊、合并冲突、评审误批、测试不稳定）
- [ ] 输出交接失败直方图
- [ ] 写入事后分析报告

---

## 练习

### ❌ 练习 1：Bug 注入探针
- [ ] `coder.py` 在 `has_bug=True` 时注入明显 bug（如 `return None` 在主体前）
- [ ] 衡量评审者误批准率
- [ ] 调整评审者提示直到误批准率 < 5%

### ❌ 练习 2：减少编码者数量
- [ ] 运行 2 个编码者（顺序运行两个子任务）
- [ ] 对比挂钟时间和通过率 vs 4 个编码者

### ❌ 练习 3：单写者约束
- [ ] 子任务触及不相交的文件集（架构师保证）
- [ ] 衡量架构师的规划负担

### ❌ 练习 4：换评审者模型
- [ ] 将评审者从 MiMo 换为其他模型
- [ ] 衡量误批准率和 Token 成本差异

### ❌ 练习 5：添加文档编写者
- [ ] 第五个角色：文档编写者（轻量模型）
- [ ] 评审后生成 changelog 条目
- [ ] 衡量文档质量是否值得额外 Token 花费

---

## 评分标准对照

| 权重 | 评分项 | 当前状态 |
|:----:|--------|----------|
| 25 | SWE-bench Pro pass@1 vs 基线 | ❌ 需要评测框架 |
| 20 | 并行加速比 | ⚠️ 有并行但无对比数据 |
| 20 | 评审质量（误批准率） | ❌ 需要 Bug 注入探针 |
| 20 | Token 效率 | ❌ 需要 Token 放大计算 |
| 15 | 协调工程 | ⚠️ 有消息板但无失败分析 |

---

## 当前进度

```
[██████████░░░░░░░░░░] 50%  构建步骤 1-6 基本完成
[░░░░░░░░░░░░░░░░░░░░]  0%  步骤 7-9（记账/评测/分析）
[░░░░░░░░░░░░░░░░░░░░]  0%  练习 1-5
```

